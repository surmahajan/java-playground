package exercise2;

public interface StringPlayground {

	Object stringManipulationUtility(Object obj);
	
	// Factory, AbstractFactory, Strategy, Builder, Decorator, Chain of Responsibility, Command, Proxy
}
